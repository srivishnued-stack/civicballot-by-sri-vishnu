import { desc, eq } from "drizzle-orm";import { db } from "@/lib/db";import { auditLogs } from "@/lib/db/schema";import { requireRole } from "@/lib/auth/session";export const dynamic='force-dynamic';
export default async function AuditPage(){const s=await requireRole('admin');const rows=await db.select().from(auditLogs).where(eq(auditLogs.organizationId,s.organizationId)).orderBy(desc(auditLogs.createdAt)).limit(500);return <><div className="page-title"><div><h2>Audit log</h2><p>Immutable record of critical administrative activity</p></div><span className="pill">Integrity monitored</span></div><div className="table-wrap"><table><thead><tr><th>Timestamp</th><th>Action</th><th>Entity</th><th>Actor</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.createdAt.toLocaleString()}</td><td><strong>{r.action}</strong></td><td>{r.entityType}</td><td>{r.actorId||'System'}</td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Audit events will appear here.</div>}</div></>}
