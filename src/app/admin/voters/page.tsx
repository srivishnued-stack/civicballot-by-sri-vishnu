import { desc, eq } from "drizzle-orm";import { db } from "@/lib/db";import { voters } from "@/lib/db/schema";import { requireRole } from "@/lib/auth/session";import { VoterImport } from "@/components/voter-import";
export const dynamic="force-dynamic";
export default async function VotersPage(){const session=await requireRole('admin');const rows=await db.select({id:voters.id,name:voters.name,voterCode:voters.voterCode,email:voters.email,department:voters.department,year:voters.year,campus:voters.campus,disabled:voters.disabled}).from(voters).where(eq(voters.organizationId,session.organizationId)).orderBy(desc(voters.createdAt)).limit(500);return <><div className="page-title"><div><h2>Voter registry</h2><p>Eligible identities and participation access</p></div></div><VoterImport/><div style={{height:16}}/><div className="table-wrap"><table><thead><tr><th>Voter</th><th>Voter ID</th><th>Department</th><th>Year</th><th>Campus</th><th>Status</th></tr></thead><tbody>{rows.map(v=><tr key={v.id}><td><strong>{v.name}</strong><br/>{v.email}</td><td>{v.voterCode}</td><td>{v.department||'—'}</td><td>{v.year||'—'}</td><td>{v.campus||'—'}</td><td><span className={`pill ${v.disabled?'pending':''}`}>{v.disabled?'Disabled':'Eligible'}</span></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">Import voters to build the election registry.</div>}</div></>}
