import Link from "next/link";import { requireRole } from "@/lib/auth/session";
export default async function Receipt({searchParams}:{searchParams:Promise<{code?:string}>}){await requireRole("voter");const {code}=await searchParams;return <main className="voter-bg"><div className="voter-wrap"><section className="election-card empty"><div className="seal" style={{margin:"0 auto 18px"}}>✓</div><div className="eyebrow">Ballot recorded</div><h1>Your vote is secure.</h1><p className="lead">Your encrypted ballot was permanently recorded. This receipt proves participation without revealing your selections.</p><div className="notice"><strong>Receipt code:</strong> {code||"Unavailable"}</div><br/><Link href="/voter" className="btn btn-secondary">Return to voter portal</Link></section></div></main>}
